<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Welcome</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
            text-align: center;
            padding: 50px;
        }

        h1 {
            color: #3498db; /* Blue color */
        }

        p {
            color: #2ecc71; /* Green color */
        }

        #usernamePlaceholder {
            font-weight: bold;
            color: #e74c3c; /* Red color */
        }

        .image-container {
            margin-top: 20px;
            position: relative;
        }

        .image-container img {
            width: 400px; /* Adjust the width as needed */
            height: 300px; /* Adjust the height as needed */
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        .text-overlay {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: #fff;
            font-size: 24px;
            font-weight: bold;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
            z-index: 2;
            animation: slideIn 1s ease-in-out;
        }

        @keyframes slideIn {
            from {
                transform: translate(-50%, -70%);
                opacity: 0;
            }
            to {
                transform: translate(-50%, -50%);
                opacity: 1;
            }
        }

        .go-back-button {
            margin-top: 20px;
            padding: 10px 20px;
            background-color: #3498db;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .go-back-button:hover {
            background-color: #2980b9;
        }
    </style>
</head>
<body>
    <h1>Welcome!</h1>
    <p>Thank you <span id="usernamePlaceholder"><%= name %></span> for visiting our website!</p>
    <div class="image-container">
        <img src="https://source.unsplash.com/random" alt="Surprise Image">
        <div class="text-overlay">Surprise Image for You!</div> 
    </div>
    <button class="go-back-button" onclick="goBack()">Go Back</button>

    <script>
        function goBack() {
            window.location.href = "/";
        }
    </script>
</body>
</html>
